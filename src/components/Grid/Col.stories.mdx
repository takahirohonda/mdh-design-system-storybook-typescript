import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { Source } from '@storybook/addon-docs/blocks';
import dedent from 'ts-dedent';

import { Col } from './Col';
import { ExcludeBottomGutter, OffsetOptions, ResponsiveBreakpoints, SpanOptions } from './types';
import { H1Elem } from '../../story-helpers/components/H1Elem';

<Meta title="Globals/Col" component={Col} />

# Col

Col component✨

This component is typically used with Row.

<Source
  language='typescript'
  code={dedent`
     <Row>
       <Col>
        <h1>Hello World</h1>
       </Col>
    </Row>
  `}
/>

## Summary

<Canvas>
  <Story 
  name="Explore"
  args={{
    alignContentX: 'left',
    alignContentY: 'top',
    excludeBottomGutter: false,
    offset: 0,
    span: 'auto',
    children: <H1Elem text='Col' />
  }}
  > 
    {(args) => <Col { ...args } />}
  </Story>
</Canvas>

<ArgsTable of={Col} />
